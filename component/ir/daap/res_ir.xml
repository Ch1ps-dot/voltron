<?xml version='1.0' encoding='UTF-8' standalone='yes'?>
<ir>
  <message name="response message of daap protocol">
  <field name="msrv" type="constant" length="B" value="msrv"/>
  <!-- Top-level container dmap.serverinforesponse ('msrv'); its 4-byte length is unreliable per spec, so the response body length is used instead. -->
  <field name="mstt" type="constant" length="B" value="mstt"/>
  <!-- Content code for dmap.status. -->
  <field name="dmap.status" type="variable" length="B" value="200"/>
  <!-- dmap.status: response status code (type long, 4 bytes); typically 200 OK. -->
  <field name="mpro" type="constant" length="B" value="mpro"/>
  <!-- Content code for dmap.protocolversion. -->
  <field name="dmap.protocolversion" type="constant" length="B" value="1.0.0"/>
  <!-- dmap.protocolversion: DMAP protocol version (type version, 4 bytes); spec currently fixes it at 1.0.0. -->
  <field name="apro" type="constant" length="B" value="apro"/>
  <!-- Content code for daap.protocolversion. -->
  <field name="daap.protocolversion" type="constant" length="B" value="1.0.0"/>
  <!-- daap.protocolversion: DAAP protocol version (type version, 4 bytes); spec currently fixes it at 1.0.0. -->
  <field name="minm" type="constant" length="B" value="minm"/>
  <!-- Content code for dmap.itemname. -->
  <field name="dmap.itemname" type="variable" length="undefined" value="Example Server"/>
  <!-- dmap.itemname: server name (type string, UTF-8, variable length determined by chunk length). -->
  <field name="mslr" type="constant" length="B" value="mslr"/>
  <!-- Content code for dmap.loginrequired. -->
  <field name="dmap.loginrequired" type="variable" length="B" value="0"/>
  <!-- dmap.loginrequired: optional char flag; if present, client must log in for queries; 0 means not required in this example. -->
  <field name="mstm" type="constant" length="B" value="mstm"/>
  <!-- Content code for dmap.timeoutinterval. -->
  <field name="dmap.timeoutinterval" type="variable" length="B" value="1800"/>
  <!-- dmap.timeoutinterval: connection timeout interval in seconds (type long, 4 bytes). -->
  <field name="msal" type="constant" length="B" value="msal"/>
  <!-- Content code for dmap.supportsautologout. -->
  <field name="dmap.supportsautologout" type="variable" length="B" value="0"/>
  <!-- dmap.supportsautologout: char flag; if present, server can automatically log clients out; 0 in this example. -->
  <field name="msup" type="constant" length="B" value="msup"/>
  <!-- Content code for dmap.supportsupdate. -->
  <field name="dmap.supportsupdate" type="variable" length="B" value="0"/>
  <!-- dmap.supportsupdate: char flag; if present, server supports /update; 0 in this example. -->
  <field name="mspi" type="constant" length="B" value="mspi"/>
  <!-- Content code for dmap.supportspersistentids. -->
  <field name="dmap.supportspersistentids" type="variable" length="B" value="0"/>
  <!-- dmap.supportspersistentids: char flag; if present, server supports persistent IDs; 0 in this example. -->
  <field name="msex" type="constant" length="B" value="msex"/>
  <!-- Content code for dmap.supportsextensions. -->
  <field name="dmap.supportsextensions" type="variable" length="B" value="0"/>
  <!-- dmap.supportsextensions: char flag; if present, server supports extensions; 0 in this example. -->
  <field name="msbr" type="constant" length="B" value="msbr"/>
  <!-- Content code for dmap.supportsbrowse. -->
  <field name="dmap.supportsbrowse" type="variable" length="B" value="0"/>
  <!-- dmap.supportsbrowse: char flag; if present, server supports browse; 0 in this example. -->
  <field name="msqy" type="constant" length="B" value="msqy"/>
  <!-- Content code for dmap.supportsquery. -->
  <field name="dmap.supportsquery" type="variable" length="B" value="0"/>
  <!-- dmap.supportsquery: char flag; if present, server supports query; 0 in this example. -->
  <field name="msix" type="constant" length="B" value="msix"/>
  <!-- Content code for dmap.supportsindex. -->
  <field name="dmap.supportsindex" type="variable" length="B" value="0"/>
  <!-- dmap.supportsindex: char flag; if present, server supports index; 0 in this example. -->
  <field name="msrs" type="constant" length="B" value="msrs"/>
  <!-- Content code for dmap.supportsresolve. -->
  <field name="dmap.supportsresolve" type="variable" length="B" value="0"/>
  <!-- dmap.supportsresolve: char flag; if present, server supports /resolve; 0 in this example. -->
  <field name="msdc" type="constant" length="B" value="msdc"/>
  <!-- Content code for dmap.databasescount. -->
  <field name="dmap.databasescount" type="variable" length="B" value="1"/>
  <!-- dmap.databasescount: number of databases known to the server (type long, 4 bytes); 1 in this example. -->
</message>
</ir>
