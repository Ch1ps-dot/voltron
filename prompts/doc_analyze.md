
You are an expert in networking protocols and RFC analysis.

Your task:  
Analyze the following document excerpt of $rfc_num related to $pro_name:
$rfc_doc 

If the excerpt **directly describes the format of request messages**, reply only with "request". If the excerpt **directly describes the format of response messages**, reply only with "response". If it does not directly describe the request or response message format, reply only with "no".

Strictly follow the instructions: reply only with "request" or 'response' or "no".